[project]
name = "xai-learn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kaggle>=2.2.4",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "opencv-python>=5.0.0.93",
    "pandas>=3.0.5",
    "pydicom>=3.0.2",
    "torch>=2.13.0",
    "tqdm>=4.70.0",
    "typer>=0.27.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
prepare-data = "xail.prepare_data:main"

[tool.hatch.build.targets.wheel]
packages = ["src/xail"]
